# Earthquake Forecasting API Environment Configuration

# API Settings
API_TITLE="Earthquake Forecasting API"
API_DESCRIPTION="API for earthquake data and forecasting system in the Philippines"
API_VERSION="1.0.0"

# Server Settings
HOST="0.0.0.0"
PORT=8000
DEBUG=true

# CORS Settings (comma-separated list)
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000,http://localhost:8080"

# Database Settings (for future use)
DATABASE_URL="sqlite:///./earthquake_data.db"

# External API Settings
PHIVOLCS_API_URL="https://earthquake.phivolcs.dost.gov.ph"
USGS_API_URL="https://earthquake.usgs.gov/fdsnws/event/1"

# ML Model Settings
MODEL_PATH="./models"
MODEL_NAME="earthquake_lstm_model.pkl"

# Logging
LOG_LEVEL="INFO"

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100
